
flex.sdk = ${FLEX_HOME}

release.name = gaffa
release.dir = ${basedir}/bin
release.version = ${version.major}.${version.minor}.${version.build}.${version.revision}

gaffa.src = ${basedir}/GA_AS3/src

